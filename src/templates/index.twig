{% extends 'layout.twig' %}

{% block content %}
    <h2>Main Page</h2>
    {% for message in flash.message("fail") %}
        <p>{{ message}}</p>
    {% endfor %}
    {% for message in flash.message("success") %}
        <p>{{ message}}</p>
    {% endfor %}

    <section id="activity">
        <h3>Activity:</h3>
    </section>
{% endblock %}
